import { ArrowUpRight, BookOpen, ShieldCheck, Users } from 'lucide-react'
import { useAuth } from '../../contexts/AuthContext'
import { usePermissions } from '../../contexts/PermissionContext'
export default function Dashboard(){const {user}=useAuth();const {permissions}=usePermissions();return <div className="space-y-8"><div><div className="text-sm six-primary-text font-semibold">Overview</div><h1 className="text-3xl font-bold mt-1">Welcome, {user?.name?.split(' ')[0]||'User'}</h1><p className="six-muted mt-1">Your Sixele LMS workspace is connected to the backend.</p></div><div className="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4"><div className="six-surface border p-5"><Users className="six-primary-text" size={21}/><div className="text-sm six-muted mt-4">Account</div><div className="font-bold mt-1">Active</div></div><div className="six-surface border p-5"><ShieldCheck className="six-primary-text" size={21}/><div className="text-sm six-muted mt-4">Permissions</div><div className="text-2xl font-bold mt-1">{permissions.size}</div></div><div className="six-surface border p-5"><BookOpen className="six-primary-text" size={21}/><div className="text-sm six-muted mt-4">Role assignments</div><div className="text-2xl font-bold mt-1">{user?.role_ids?.length||0}</div></div><div className="six-surface border p-5"><div className="text-sm six-muted">API session</div><div className="text-2xl font-bold mt-1">JWT</div><div className="text-xs six-primary-text mt-1">Authenticated</div></div></div><div className="grid lg:grid-cols-3 gap-6"><div className="lg:col-span-2 six-surface border p-6"><div className="flex justify-between items-center"><div><h2 className="font-bold text-lg">Your access</h2><p className="text-sm six-muted">Permissions are supplied by the backend and used by the UI for navigation.</p></div><ArrowUpRight className="six-primary-text" size={20}/></div><div className="flex flex-wrap gap-2 mt-6">{[...permissions].slice(0,18).map(p=><span key={p} className="text-xs px-3 py-2 rounded-lg" style={{background:'var(--hover)',color:'var(--muted)'}}>{p}</span>)}{permissions.size>18&&<span className="text-xs six-muted px-2 py-2">+{permissions.size-18} more</span>}</div></div><div className="rounded-2xl p-6 text-white" style={{background:'var(--sidebar)'}}><h2 className="font-bold text-lg">Frontend v0.3</h2><p className="text-white/60 text-sm mt-2">Authentication and API foundation are connected.</p><div className="mt-6 space-y-3 text-sm"><div className="flex justify-between"><span>JWT login</span><span className="text-green-300">Ready</span></div><div className="flex justify-between"><span>Current user</span><span className="text-green-300">Ready</span></div><div className="flex justify-between"><span>Permissions</span><span className="text-green-300">Ready</span></div><div className="flex justify-between"><span>Role API</span><span className="text-green-300">Ready</span></div></div></div></div></div>}
