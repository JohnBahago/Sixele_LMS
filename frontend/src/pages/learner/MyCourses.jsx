import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { BookOpen, Search } from 'lucide-react'; import { api } from '../../services/api'
export default function MyCourses(){const [items,setItems]=useState([]),[q,setQ]=useState(''),[status,setStatus]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);api.learnerCourses(status).then(setItems).finally(()=>setLoading(false))},[status]);const filtered=items.filter(x=>(x.title+' '+x.category).toLowerCase().includes(q.toLowerCase()));return <div className="space-y-6"><div><h1 className="text-3xl font-bold">My Courses</h1><p className="six-muted mt-1">All courses you are enrolled in.</p></div><div className="flex flex-col md:flex-row gap-3"><div className="flex-1 relative"><Search className="absolute left-3 top-3.5 six-muted" size={18}/><input className="six-input rounded-xl w-full pl-10 pr-4 py-3" placeholder="Search courses…" value={q} onChange={e=>setQ(e.target.value)}/></div><select className="six-input rounded-xl px-4 py-3" value={status} onChange={e=>setStatus(e.target.value)}><option value="">All courses</option><option value="active">Active</option><option value="completed">Completed</option></select></div>{loading?<div className="six-muted">Loading courses…</div>:<div className="grid md:grid-cols-2 xl:grid-cols-3 gap-5">{filtered.map(c=><Link key={c.enrollment_id} to={`/learn/${c.enrollment_id}`} className="six-surface border p-5 six-hover"><BookOpen className="six-primary-text"/><h2 className="font-bold text-lg mt-4">{c.title}</h2><p className="six-muted text-sm mt-1 line-clamp-3">{c.short_description}</p><div className="mt-5 h-2 rounded-full" style={{background:'var(--hover)'}}><div className="six-primary-bg h-2 rounded-full" style={{width:`${Math.min(100,c.progress_percent)}%`}}/></div><div className="flex justify-between text-xs six-muted mt-2"><span>{Number(c.progress_percent).toFixed(0)}%</span><span>{c.enrollment_status}</span></div></Link>)}{!filtered.length&&<div className="six-muted">No courses found.</div>}</div>}</div>}
